# 用法说明：
# - .env.example：模板，列出有哪些变量、默认值是什么；可提交到 git，不要写真实密钥。
# - .env.local：你本机真实配置（复制本文件改名或按需填写），Next.js 会读取且默认不提交 git。
#   改完 .env.local 后需重启 `npm run dev` 才会生效。

ZHIPU_API_KEY=your_zhipu_api_key_here
# --- 智谱模型分工与 TTFT（首 token 延迟）---
# ZHIPU_ANALYSIS_MODEL：第一段，非流式 JSON（stage/情绪/写档标记等），短 prompt；首包前必须等它跑完。
# ZHIPU_CHAT_MODEL：第二段，流式正文；system 含完整「角色.md」+ RAG 片段，token 大则略慢。
# 体感「慢」多半来自：第一段整段返回 + 网络 RTT；流式只发生在第二段。两段默认用 flash 可兼顾速度与成本。
ZHIPU_CHAT_MODEL=glm-4-flash
ZHIPU_ANALYSIS_MODEL=glm-4-flash
ZHIPU_EMBED_MODEL=embedding-2
# RAG：keyword=本地关键词（默认，无额外 API）；vector=向量检索（会调用 embedding）
RAG_MODE=keyword
# 设为 1 时允许流式区分「思维过程」与「最终回复」（前端可再关 showReasoning）
SHOW_AGENT_REASONING=0
# 仅本地开发：设为 1 时允许 GET /api/archive、/api/memory 使用 ?userId= 调试（勿在生产开启）
ALLOW_DEBUG_USER_ID=0
